{
  "name": "mutant-coverage",
  "version": "1.0.0",
  "main": "dist/index.js",
  "author": "wait_what_",
  "license": "AGPL-3.0-only",
  "repository": "https://github.com/mutantremix/coverage.git",
  "devDependencies": {
    "@types/node": "^14.14.10",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "log75": "^2.1.0"
  },
  "scripts": {
    "build": "rm -rf dist/* && tsc",
    "start": "yarn build && node dist/index"
  }
}
